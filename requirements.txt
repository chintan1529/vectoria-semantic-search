# Vectoria — Dependencies
# Minimal footprint for CPU-only, 8GB RAM environments

# Core: Embedding model
sentence-transformers>=2.2.0

# Core: Vector similarity search (CPU build)
faiss-cpu>=1.7.0

# Core: Array operations
numpy>=1.24.0

# Dataset: Wikipedia article fetching
wikipedia>=1.4.0

# Vectoria — Dependencies
# Vectoria — Dependencies
# Minimal footprint for CPU-only, 8GB RAM environments

# Core: Embedding model
sentence-transformers>=2.2.0

# Core: Vector similarity search (CPU build)
faiss-cpu>=1.7.0

# Core: Array operations
numpy>=1.24.0

# Dataset: Wikipedia article fetching
wikipedia>=1.4.0

# API Backend
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
pydantic-settings>=2.3.4
python-dotenv>=1.0.1
sse-starlette>=2.1.2
structlog>=24.1.0
aiolimiter>=1.1.0
cachetools>=5.3.3
rank_bm25>=0.2.2

# Observability (Phase 1)
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
langfuse>=2.31.0
prometheus-client>=0.20.0

